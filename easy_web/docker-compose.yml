version: '2'
services:
  # 基本环境
  service: # 容器名字
    # build 该置顶目录下的dockerfile
    build: .
    # image指定build Dockerfile生成镜像的名称
    image: easy_web
    ports:
        - "9999:80"
    command: 
        - /var/www/run.sh
    stdin_open: true
    tty: true